[package]
name = "Welcome"
version = "0.1.0"
edition = "2021"

[dependencies]
#artisan_middleware = "7.0.0"
# TEMPORARY local path for the resource-taxonomy migration -- 7.0.0 isn't
# published yet, and /opt/artisan/lib/artisan_lib doesn't exist on a dev
# machine either way (this crate could not build as checked in). MUST be
# reverted to the registry pin above before this is ever pushed/merged.
artisan_middleware = { path = "../../Libraries/artisan_lib" }
colored = "2.1.0"
#dusa_collection_utils = "3.0.8"
gethostname = "0.5.0"
lsb_release = "0.1.0"
simple_pretty = "0.1.0"
tokio = "1.42.0"
